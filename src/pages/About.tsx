import React from 'react';
import { Header } from '../components/Header';

export const About = () => {
    return (
        <section>
            <Header/>
            <p>This is the about page</p>
        </section>
    )
}